apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-application.fullname" . }}
  labels:
    {{- include "flask-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-application.selectorLabels" . | nindent 4 }}
